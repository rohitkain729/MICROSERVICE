#service id
spring.application.name=Billing-Service

#server  port
server.port=9095

#instance id
eureka.instance.instance-id=${spring.application.name}:${random.value}

#register with eureka server url
eureka.client.service-url.default-zone=http://localhost:8761/eureka
